[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    network: Tests requiring live network access to external APIs.
              Run with: pytest -m network
              Requires: api.apps1.nsw.gov.au in the outbound proxy allowlist.
